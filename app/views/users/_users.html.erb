<% if users.any? %>
<ul>
  <% users.each do |user| %>
  <li>
    <div>
        <%= user.name %>
    </div>
    <div>
        <p><%= link_to 'プロフィールを表示する。', @task %></p>
    </div>
  </li>
  <% end %>
</ul>
<%= paginate users %>
<% end %>