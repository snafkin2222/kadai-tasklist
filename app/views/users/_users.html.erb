<% if tasks.any? %>
  <ul>
    <% tasks.each do |task| %>
      <li>
          <div>
            <%= user.name %>
          </div>
            <p><%= link_to 'View profile', task_path(task) %></p>
      </li>
    <% end %>
  </ul>
  <%= paginate tasks %>
<% end %>